CONTRIBUTING.md - GitFlow 团队协作规范

## 前言

本文档为项目统一 **GitFlow 分支管理与团队协作规范**，适用于所有参与本项目开发、维护、提交代码的人员。目的是统一分支策略、开发流程、提交规范与 PR 合并规则，保证代码仓库整洁、版本稳定、可追溯，适配中大型项目、多迭代、多版本发布场景。

所有代码提交、分支创建、PR 合并均需严格遵守本规范。

---

## 一、分支整体说明（GitFlow 标准模型）

本项目采用**标准 GitFlow 工作流**，包含 **2 条长期常驻分支** + **4 类临时特性分支**，所有临时分支开发完成后均会合并删除，仅保留常驻分支。

### 1. 长期常驻分支（永久保留、受保护）

* **main**
  * 定位：**线上生产环境分支**
  * 特性：始终稳定、可直接部署、无未完成功能、无已知 bug
  * 规则：禁止直接 push、禁止直接修改，仅通过 PR 合并
  * 版本：每一次合并至 main 均需打正式版本 Tag（vX.Y.Z）
* **develop**
  * 定位：**日常开发集成分支**，下个版本的开发基线
  * 特性：汇总所有新功能、优化迭代，包含待发布功能
  * 规则：禁止直接 push，所有功能通过 PR 合并进入

### 2. 临时分支（开发完毕即删除）

* **feature/\***：新功能开发分支
* **bugfix/\***：迭代内普通 bug 修复分支
* **release/\***：版本发布准备分支
* **hotfix/\***：线上紧急故障修复分支

---

## 二、分支命名规范（强制统一）

通用格式：`类型/[issue-ID-]简短描述`，全部小写、中横线分隔、语义清晰、不使用中文、不使用特殊符号。

### 1. feature 新功能分支

从 `develop` 拉出，用于开发新业务功能

示例：

* `feature/101-user-login`
* `feature/102-order-pay`

### 2. bugfix 迭代修复分支

从 `develop` 拉出，修复测试/开发阶段发现的问题（非线上紧急 bug）

示例：

* `bugfix/103-fix-avatar-display`
* `bugfix/104-fix-form-verify`

### 3. release 版本发布分支

从 `develop` 拉出，用于版本公测、提测、收尾修复，不新增大功能

示例：

* `release/v1.2.0`
* `release/v1.3.0`

### 4. hotfix 线上紧急修复分支

从 `main` 拉出，修复线上紧急故障、崩溃、安全漏洞

示例：

* `hotfix/105-fix-online-crash`
* `hotfix/106-security-patch`

### 5. 辅助分支规范

* `docs/*`：文档修改、注释优化
* `chore/*`：工程配置、依赖更新、脚本优化、格式化
* `refactor/*`：代码重构、逻辑优化（无功能变更）

---

## 三、完整开发流程（GitFlow 标准流程）

### 1. 新功能开发流程（feature）

1. 基于最新 `develop` 拉取功能分支：`feature/xxx`
2. 本地开发、自测、提交代码
3. 推送远端，发起 **PR 到 develop**
4. 代码审核、CI 校验、测试验证
5. 审核通过后合并入 `develop`
6. 合并完成后**删除该 feature 分支**

### 2. 迭代 bug 修复流程（bugfix）

1. 基于最新 `develop` 拉取修复分支：`bugfix/xxx`
2. 修复问题、自测通过
3. 发起 PR 到`develop`
4. 审核合并后删除分支

### 3. 版本发布流程（release）

1. 迭代功能全部合入 `develop` 后，从 develop 拉出 `release/vX.Y.Z`
2. 提测、进行版本测试，仅做 bug 修复，不新增功能
3. 测试通过后，**同时合并至 main、develop**
4. 在 main 分支打对应版本 Tag（`vX.Y.Z`）
5. 删除 release 分支

### 4. 线上紧急修复流程（hotfix）

1. 基于当前最新 `main` 拉出 `hotfix/xxx` 分支
2. 紧急修复、自测、线上验证
3. 先后发起 PR：先合 `main`、再合 `develop`（保证后续版本不回归）
4. main 分支更新补丁版本 Tag（如 v1.2.1）
5. 删除 hotfix 分支

---

## 四、Commit 提交信息规范

所有 git commit 信息必须遵循语义化规范，格式：`type(scope): message`

### 常用 type 类型

* **feat**：新增功能
* **fix**：修复 bug
* **docs**：文档更新
* **refactor**：代码重构（无功能变化）
* **perf**：性能优化
* **test**：新增/修改测试用例
* **chore**：工程配置、依赖、脚本调整

### 规范示例

* `feat(user): 新增手机号登录功能`
* `fix(order): 修复订单金额计算错误`
* `docs: 更新部署文档`

---

## 五、Pull Request（PR）审核规范

### 1. PR 基础规则

* 禁止跨分支乱提 PR：feature/bugfix 只能提向 develop；hotfix/release 按流程提向 main/develop
* 所有 PR 必须关联对应 Issue 工单
* 所有 PR 必须通过 CI 校验（编译、 lint、单元测试）
* 必须至少 1 名负责人审核通过方可合并

### 2. PR 描述模板（必填）

```Plain
## 变更内容
简要描述本次代码改动内容

## 关联工单
Closes #xxx

## 自测情况
- [ ] 本地自测通过
- [ ] 功能正常无回归
- [ ] 符合代码规范

## 特殊说明
有无兼容性、配置、环境变更说明
```

### 3. 合并方式规范

* 统一使用 **Squash and merge**：压缩为单条清晰 commit，保证分支历史干净
* 合并后**必须删除源分支**

---

## 六、分支保护与禁止操作（强制）

仓库已对 `main`、`develop` 开启分支保护，所有人员严格遵守：

* 禁止直接 push 代码到 main / develop
* 禁止强制推送（force push）到常驻分支
* 禁止随意删除常驻分支
* 禁止在 release/hotfix 分支新增非必要功能
* 禁止无工单、无说明的代码提交与合并

---

## 七、版本 Tag 规范

采用 **语义化版本号**：`主版本.次版本.补丁版本`（X.Y.Z）

* **X 主版本**：不兼容架构升级、重大功能重构
* **Y 次版本**：迭代新增功能、版本更新
* **Z 补丁版本**：线上 bug 修复、小优化

示例：`v1.0.0`、`v1.1.0`、`v1.1.1`

---

## 八、异常处理规则

1. **代码冲突**：由当前分支开发者本地拉取目标分支最新代码、解决冲突、自测后重新推送
2. **CI 失败**：未修复禁止合并，禁止绕过检查强制合并
3. **审核驳回**：按评审意见修改完成后，重新提交等待审核
4. **线上问题**：一律走 hotfix 流程，禁止直接改 main 或合并未测试代码

---

## 九、附则

本规范适用于项目所有开发、测试、运维人员，自生效之日起，所有代码提交、分支管理、PR 合并均需严格执行，团队定期合规检查，保证项目版本稳定、迭代有序。
